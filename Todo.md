4. in samba file explorer, if I copy under same folder, I shall create file like _1, _2 automatically, now I got an error
5. a little bug on the breadcrumb, when I click to edit it, if you don't type anything and click the button to go to parent folder, it continues display the previous folder

7. in ftp mode, copy doesn't work
8. when double click a file, open it in edit mode if it is a txt file. otherwise download it
9. be able to split the window and show 2 tabs
10. in settings, the group and tab config is ugly, can we make it better